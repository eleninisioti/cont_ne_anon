"""The neuro-evolution search algorithms, independent of any task.

GA, DNS and ES. `es.py` is the whole distribution-based arm: NES and OpenES are
two settings of one class, and both studies in this repo -- the benchmarking
paper through `EvosaxES`, the generalists study through `ES` and `MultiES` --
drive that one implementation.
"""
